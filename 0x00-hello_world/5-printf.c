#include <stdio.h>
/**
 * main - is the main functionand calls other functions
 *
 * Return: always return zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
